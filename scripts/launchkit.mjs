#!/usr/bin/env node

/**
 * LaunchKit CLI
 *
 * For full CLI functionality, use the /cli package.
 * This script just shows how to get started.
 */

console.log(`
🚀 LaunchKit

To build your idea:
  1. Open VS Code
  2. Open Copilot Chat (Cmd+Shift+I)
  3. Type: launchkit

For CLI commands, install the full CLI:
  npm install -g create-launchkit
  launchkit init my-project
`);
