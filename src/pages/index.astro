---
export const prerender = true;
// Language selector - redirect to default locale or show selector
import { defaultLocale } from "@/i18n/utils";

// Redirect to default locale
return Astro.redirect(`/${defaultLocale}`);
---
